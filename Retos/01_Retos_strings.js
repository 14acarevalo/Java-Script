// Transformaciones de texto - Escribe una función que reciba un nombre completo como entrada y devuelva el nombre con la primera letra de cada palabra en mayúscula.

let nombre = "ALberTo CamPanerO"

//Transformación a mayúsuclas
let mayusculas = nombre.toUpperCase()
console.log(`Transformación en mayúsculas ${mayusculas}`)
let minusculas = nombre.toLowerCase()
console.log(`Transformación en minúsculas ${minusculas}`)

//Vamos con el ejercicio

let name = "alberto campanero"
console.log(name.replace("a", "A"))