/**
 * Ejercicio 4: Uso de las llaves
Si una función de flecha tiene múltiples líneas de código, debes usar las llaves {}. Crea una función que devuelva el producto de tres números.
 */

const operacion = (a,b,c) =>{
    const multiplicacion = a*b*c
    return multiplicacion
}

console.log(operacion(2,3,4))